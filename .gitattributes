# Normalise line endings in the repo; check out native on each platform.
* text=auto

# Keep LF in files that break with CRLF.
*.sh text eol=lf

# Binary — never diff or convert.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
